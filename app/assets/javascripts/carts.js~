Depot = { 
  new_order: function(url){
    console.log(url);
  var div = document.createElement('div');
    $(div).load(url);
    $(div).dialog({
      autoOpen: true,
      height: 500,
      width: 600,
      modal: true,
     title: 'new order'
    });
  }
  
} 